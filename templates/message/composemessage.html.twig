{% block body %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate', 'class': 'msgForm'}}) }}
        {{ form_widget(form) }}
        <button class="button success">{{ button_label|default('Reply') }}</button>
        <a data-close href="#" class="button primary">Cancel</a>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    <script>
        $(".chosen-select").chosen();
    </script>
{% endblock %}