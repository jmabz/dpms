{% extends 'base.html.twig' %}
{% block title %}Add Clinic{% endblock %}
{% block body %}
	<div class="grid-container">
		<h1>Add Clinic</h1>
		{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
			{{ form_widget(form) }}

			{# {{ form_widget(form.doctors, {'attr' : 
			{'data-prototype' : '{{ form_widget(form.doctors.vars.prototype)|e(\'html_attr\') }}', 
			'data-index':'{{ form.doctors|length }}',

			}}) }} #}
			{# <div class="js-clinic-doctor-wrapper" data-prototype="{{ form_widget(form.doctors.vars.prototype)|e('html_attr') }}" data-index="{{ form.doctors|length }}">
				{% for doctor in form.doctors %}
					<div class="js-clinic-doctor-item">
						<a href="#" class=" js-remove-doctor pull-right">Remove</a>
						{{ form_row(doctor.id) }}
					</div>
				{% endfor %}
				<a href="#" class="js-clinic-doctor-add">Add Doctor</a>
			
			</div> #}

			<input type="submit" class="button success" value="Add Clinic">
			<a href="{{ path('clinic_list') }}" class="button">Cancel</a>
		{{ form_end(form) }}
	</div>

{% endblock %}
{% block javascripts %}
	{{ parent() }}
	<script>
        jQuery(document).ready(function() {
            var $wrapper = $('.js-clinic-doctor-wrapper');
            $wrapper.on('click', '.js-remove-doctor', function(e) {
                e.preventDefault();
                $(this).closest('.js-clinic-doctor-item')
                    .fadeOut()
                    .remove();
            });
            $wrapper.on('click', '.js-clinic-doctor-add', function(e) {
                e.preventDefault();
                // Get the data-prototype explained earlier
                var prototype = $wrapper.data('prototype');
                // get the new index
                var index = $wrapper.data('index');
                // Replace '__name__' in the prototype's HTML to
                // instead be a number based on how many items we have
                var newForm = prototype.replace(/__name__/g, index);
                // increase the index with one for the next item
                $wrapper.data('index', index + 1);
                // Display the form in the page before the "new" link
                $(this).before(newForm);
            });
		});		
	</script>
{% endblock %}